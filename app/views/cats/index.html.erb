<h1>Here Are All Our Cats</h1>

<ul>
  <% @cats.each do |cat| %>
    <li>
      <a href="<%= cat_url(cat.id) %>">
        <%= cat.name %>
      </a>
    </li>
  <% end %>
</ul>

<a href="<%= new_cat_url %>">Add a New Cat!</a>
<br>
<a href="<%= cat_rental_requests_url %>">Go to Cat Rentals</a>
